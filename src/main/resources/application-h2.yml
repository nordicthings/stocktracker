spring:
  datasource:
    url: jdbc:h2:file:./.local/h2/stocktracker;MODE=MariaDB;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
